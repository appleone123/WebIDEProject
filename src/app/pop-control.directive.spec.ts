import { PopControlDirective } from './pop-control.directive';

describe('PopControlDirective', () => {
  it('should create an instance', () => {
    const directive = new PopControlDirective();
    expect(directive).toBeTruthy();
  });
});
